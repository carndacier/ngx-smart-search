import { SmartSearchDirective } from './smart-search.directive';

describe('SmartSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new SmartSearchDirective();
    expect(directive).toBeTruthy();
  });
});
